npm install webpack -g

npm install -g typescript

npm link typescript

//Open the cmd with the path set to root od the project and build the project using webpack
webpack

//Open the cmd with the path set to root od the project and start the server
npm start
